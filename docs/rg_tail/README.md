# RG-Tail Notes

This directory collects the standalone documentation for the experimental
RG-tail waveform work in `src/rg_tail/`.

Current primary note:

- `rg_tail_waveform_notes.tex` / `rg_tail_waveform_notes.pdf`
  Consolidated summary of the current `waveform.py` implementation, its
  relation to arXiv:2602.08833, and the present 22-only balance-law
  Fourier-domain prescription used by the Fisher workflow.

Build artifacts such as `.aux`, `.log`, and `.out` are intentionally ignored.
