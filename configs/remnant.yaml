name: remnant
description: "Final mass, spin, and kick velocity prediction"

# Cost parameters
t0: 0.0001      # 0.1 ms per point (in seconds)
alpha: 0.05

# Input parameter ranges
parameters:
  q: [1.0, 10.0]
  chi1_mag: [0.0, 0.8]
  chi2_mag: [0.0, 0.8]

# Dataset
dataset:
  n_train: null
  n_test: null
  held_out_regions: []
