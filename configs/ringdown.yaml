name: ringdown
description: "Quasi-normal mode frequency prediction"

# Cost parameters
t0: 0.00001     # 0.01 ms per query (in seconds)
alpha: 0.03

# Input parameter ranges
parameters:
  chi_f: [0.0, 0.99]
  l_max: 4
  n_max: 2

# Dataset
dataset:
  n_train: null
  n_test: null
  held_out_regions: []
