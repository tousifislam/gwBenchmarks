name: ringdown
description: "Quasi-normal mode frequency prediction"


# Input parameter ranges
parameters:
  chi_f: [0.0, 0.99]
  l_max: 4
  n_max: 2

# Dataset
dataset:
  n_train: null
  n_test: null
  held_out_regions: []
